import FormTransferCard from "@/components/stockTransfer/FormTransferCard";

export const metadata = {
  title: 'Form Transfer',
  description: 'Form Transfer page',
}

const Order = () => {
    return ( 
      <div>
        <FormTransferCard/>
      </div>
    );
  }
  
  export default Order;